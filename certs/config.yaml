# Mitmproxy Configuration
# Allow connections from outside localhost
block_global: false
block_private: false

# Proxy settings
listen_host: 0.0.0.0
listen_port: 8080

# TLS/SSL settings
# Certificates will be auto-generated in this directory
